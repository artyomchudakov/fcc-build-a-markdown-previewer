import { ProjectDescription } from './ProjectDescription';
import './style.css';

export default function MarkdownPreviewer() {
  return (
    <section>
      <div>Build a Markdown Previewer</div>
      <ProjectDescription name="Build a Markdown Previewer" />
    </section>
  );
}
